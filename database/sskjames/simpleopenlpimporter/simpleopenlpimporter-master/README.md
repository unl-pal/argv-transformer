# Simple OpenLP Importer
This is a little command line Java application that can be used to import songs from text files into a OpenLP
database (sqlite).

The file name is assumed as the song title. 
