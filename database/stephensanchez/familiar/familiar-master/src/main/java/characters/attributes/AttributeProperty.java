package characters.attributes;

/**
 * A {@link Attribute} may have any number of numeric, text, or boolean values associated with it
 * to define its value for a particular {@link characters.Character}. The AttributeProperty defines each of
 * these values based on their {@link AttributeType} and value.
 */
public interface AttributeProperty {
}
