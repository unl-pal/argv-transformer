package characters.attributes;

/**
 * Each {@link characters.Character} has a set of {@link Attribute}s. These attributes are each
 * associated with a {@link AttributeType} which defines each
 * {@link AttributeProperty} associated with an attribute.
 */
public interface AttributeType {
}
