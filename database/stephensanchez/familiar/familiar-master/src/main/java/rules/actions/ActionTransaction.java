package rules.actions;

/**
 * Since actions are initiated by live players, it is important to keep a persistent record of the actions taken during
 * a game.
 */
public interface ActionTransaction {
}
